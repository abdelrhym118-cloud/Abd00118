name: ضغط الفيديو (AV1 - 4 خيارات بدون سقف)

on:
  workflow_dispatch:
    inputs:
      message_id:
        description: 'معرف رسالة الفيديو في تيليجرام (اتركه فارغًا إذا استخدمت url)'
        required: false
        default: ''
      url:
        description: 'رابط الفيديو المباشر (اتركه فارغًا إذا استخدمت message_id)'
        required: false
        default: ''
      resolution:
        description: 'الدقة (مثلاً 720)'
        required: true
        default: '720'
      codec:
        description: 'الكوديك (av1)'
        required: false
        default: 'av1'
      content_type:
        description: 'نوع المحتوى (real, animation, animation_ultra, auto)'
        required: true
        default: 'real'
      chat_id:
        description: 'معرف الدردشة (نفسه مصدر الفيديو ووجهة الإشعارات)'
        required: true
      private:
        description: 'خصوصي (true/false)'
        required: true
      password:
        description: 'كلمة المرور (فقط لو خاص)'
        required: false
        default: ''
      filename:
        description: 'اسم الملف المطلوب'
        required: false
        default: ''

concurrency:
  group: video-compression
  cancel-in-progress: false
  queue: max

permissions:
  contents: write

jobs:
  split-video:
    runs-on: ubuntu-latest
    timeout-minutes: 360
    outputs:
      chunks: ${{ steps.segment.outputs.chunks }}
      video_title: ${{ steps.title.outputs.video_title }}
    steps:
      - name: استنساخ المستودع
        uses: actions/checkout@v4

      - name: تجهيز أدوات التحميل
        run: |
          echo "تجهيز البيئة" > stage.txt
          sudo apt update
          sudo apt install -y jq ffmpeg
          pip install telethon

      - name: تحميل الفيديو المصدر (تيليجرام عبر Telethon أو رابط مباشر)
        env:
          TG_API_ID: ${{ secrets.TG_API_ID }}
          TG_API_HASH: ${{ secrets.TG_API_HASH }}
          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
          SOURCE_CHAT_ID: ${{ github.event.inputs.chat_id }}
          MESSAGE_ID: ${{ github.event.inputs.message_id }}
          VIDEO_URL: ${{ github.event.inputs.url }}
        run: |
          echo "تحميل الفيديو" > stage.txt
          if [ -n "$MESSAGE_ID" ]; then
            echo "📥 تحميل الفيديو عبر Telethon (جلسة بوت) من الرسالة رقم $MESSAGE_ID"
            python3 <<'PYEOF'
          import os, asyncio, sys
          from telethon import TelegramClient
          from telethon.sessions import StringSession

          api_id = int(os.environ["TG_API_ID"])
          api_hash = os.environ["TG_API_HASH"]
          bot_token = os.environ["BOT_TOKEN"]
          chat_id = int(os.environ["SOURCE_CHAT_ID"])
          message_id = int(os.environ["MESSAGE_ID"])

          async def main():
              client = TelegramClient(StringSession(), api_id, api_hash)
              await client.start(bot_token=bot_token)
              async with client:
                  msg = await client.get_messages(chat_id, ids=message_id)
                  if msg is None or not msg.media:
                      print("❌ لم يتم العثور على رسالة تحتوي على ملف بهذا المعرف")
                      sys.exit(1)
                  path = await client.download_media(msg, file="raw_input.mp4")
                  if not path:
                      print("❌ فشل تحميل الملف")
                      sys.exit(1)
                  print(f"✅ تم التحميل: {path}")

          asyncio.run(main())
          PYEOF
          elif [ -n "$VIDEO_URL" ]; then
            echo "📥 تحميل الفيديو عبر الرابط المباشر..."
            curl -L -o raw_input.mp4 "$VIDEO_URL" || { echo "❌ فشل التحميل"; exit 1; }
          else
            echo "❌ لم يتم تمرير message_id ولا url"; exit 1
          fi
          if [ ! -f raw_input.mp4 ]; then echo "❌ الملف لم يُنشأ"; exit 1; fi
          echo "✅ تم التحميل بنجاح"

      - name: استخراج وتنظيف اسم الفيديو الأصلي
        id: title
        run: |
          CLEAN_TITLE="video"
          echo "video_title=$CLEAN_TITLE" >> "$GITHUB_OUTPUT"
          echo "📝 العنوان المستخدم: $CLEAN_TITLE"

      - name: تقطيع الفيديو (فقط لو أطول من 20 دقيقة)
        id: segment
        run: |
          mkdir -p chunks_raw
          DURATION=$(ffprobe -v error -show_entries format=duration -of csv=p=0 raw_input.mp4 | cut -d. -f1)
          echo "⏱️ مدة الفيديو: ${DURATION} ثانية"
          if [ "$DURATION" -gt 1200 ]; then
            echo "📦 الفيديو طويل، سيتم تقطيعه للمعالجة المتوازية"
            ffmpeg -i raw_input.mp4 -c copy -f segment -segment_time 600 -reset_timestamps 1 "chunks_raw/chunk_%03d.mp4"
          else
            echo "📄 الفيديو قصير، سيُعالج كجزء واحد فقط"
            cp raw_input.mp4 "chunks_raw/chunk_000.mp4"
          fi
          CHUNKS_JSON=$(ls chunks_raw/chunk_*.mp4 | sed 's/chunks_raw\/chunk_//' | sed 's/\.mp4//' | jq -R . | jq -s -c .)
          echo "chunks=$CHUNKS_JSON" >> "$GITHUB_OUTPUT"
          echo "الأجزاء: $CHUNKS_JSON"

      - name: رفع الأجزاء الخام كـ Artifacts
        uses: actions/upload-artifact@v4
        with:
          name: raw-chunks
          path: chunks_raw/
          retention-days: 1

  encode-chunks:
    needs: split-video
    runs-on: ubuntu-latest
    timeout-minutes: 360
    strategy:
      matrix:
        chunk: ${{ fromJSON(needs.split-video.outputs.chunks) }}
      max-parallel: 10
    steps:
      - name: استعادة بناء SVT-AV1 و FFmpeg من الكاش (أو بناؤه)
        uses: actions/cache@v4
        id: cache-build
        with:
          path: ~/av1-build
          key: av1-ffmpeg-simple-420-${{ runner.os }}-v1

      - name: تثبيت متطلبات التشغيل الأساسية
        run: |
          sudo apt update
          sudo apt install -y libopus0

      - name: بناء SVT-AV1 4.2.0 و FFmpeg (بناء بسيط)
        if: steps.cache-build.outputs.cache-hit != 'true'
        run: |
          set -euo pipefail
          sudo apt install -y cmake nasm libopus-dev
          rm -rf SVT-AV1 FFmpeg

          git clone --depth 1 --branch v4.2.0 https://gitlab.com/AOMediaCodec/SVT-AV1.git
          cd SVT-AV1/Build
          cmake .. -DCMAKE_BUILD_TYPE=Release -DBUILD_SHARED_LIBS=ON -DCMAKE_INSTALL_PREFIX="$HOME/av1-build"
          make -j$(nproc)
          make install
          cd ../..

          git clone --depth 1 --branch release/8.0 https://github.com/FFmpeg/FFmpeg.git
          cd FFmpeg
          export PKG_CONFIG_PATH="$HOME/av1-build/lib/pkgconfig:${PKG_CONFIG_PATH:-}"
          export LD_LIBRARY_PATH="$HOME/av1-build/lib"
          ./configure --prefix="$HOME/av1-build" \
            --enable-gpl \
            --enable-version3 \
            --enable-libsvtav1 \
            --enable-libopus
          make -j$(nproc)
          make install
          cd ..

      - name: تفعيل مسار FFmpeg المخصص
        run: |
          echo "$HOME/av1-build/bin" >> "$GITHUB_PATH"
          echo "LD_LIBRARY_PATH=$HOME/av1-build/lib:${LD_LIBRARY_PATH:-}" >> "$GITHUB_ENV"

      - name: التحقق الإلزامي من سلامة البناء
        run: |
          set -euo pipefail
          echo "🔍 نسخة ffmpeg:"
          which ffmpeg
          ffmpeg -version

      - name: تنزيل الجزء المخصص
        uses: actions/download-artifact@v4
        with:
          name: raw-chunks
          path: chunks_raw

      - name: ضغط الجزء (${{ matrix.chunk }}) - حسب نوع المحتوى
        env:
          RESOLUTION: ${{ github.event.inputs.resolution }}
          CONTENT_TYPE: ${{ github.event.inputs.content_type }}
          CHUNK_ID: ${{ matrix.chunk }}
        run: |
          set -o pipefail
          echo "📥 القيمة المستلمة من البوت: '$CONTENT_TYPE'"

          IN="chunks_raw/chunk_${CHUNK_ID}.mp4"
          OUT_DIR="chunks_encoded"
          OUT_FILE="${OUT_DIR}/encoded_${CHUNK_ID}.mkv"
          mkdir -p "$OUT_DIR"

          case "$CONTENT_TYPE" in
            real)
              CRF=30
              PRESET=6
              AUDIO_BITRATE=32
              VF_CHAIN="scale=-2:${RESOLUTION}"
              ;;
            animation)
              CRF=50
              PRESET=7
              AUDIO_BITRATE=32
              VF_CHAIN="scale=-2:${RESOLUTION}"
              ;;
            animation_ultra)
              CRF=52
              PRESET=7
              AUDIO_BITRATE=32
              VF_CHAIN="scale=-2:${RESOLUTION}"
              ;;
            auto)
              CRF=45
              PRESET=7
              AUDIO_BITRATE=32
              VF_CHAIN="scale=-2:${RESOLUTION}"
              ;;
            *)
              echo "⚠️ قيمة غير معروفة، استخدام الإعدادات الافتراضية"
              CRF=40
              PRESET=7
              AUDIO_BITRATE=32
              VF_CHAIN="scale=-2:${RESOLUTION}"
              ;;
          esac

          echo "🎛️ النوع: $CONTENT_TYPE | CRF=$CRF | preset=$PRESET | صوت ${AUDIO_BITRATE}k"

          ffmpeg -y -i "$IN" \
            -vf "$VF_CHAIN" \
            -c:v libsvtav1 \
            -crf $CRF \
            -preset $PRESET \
            -svtav1-params tune=0 \
            -c:a libopus \
            -b:a "${AUDIO_BITRATE}k" \
            -y "$OUT_FILE" 2>&1 | tee "ffmpeg_${CHUNK_ID}.log"

          if [ ! -s "$OUT_FILE" ]; then
            echo "❌ فشل ترميز الجزء ${CHUNK_ID}"
            cat "ffmpeg_${CHUNK_ID}.log"
            exit 1
          fi
          echo "✅ تم إنشاء $OUT_FILE ($(stat -c%s "$OUT_FILE") بايت)"

      - name: رفع الجزء المضغوط
        uses: actions/upload-artifact@v4
        with:
          name: encoded-${{ matrix.chunk }}
          path: chunks_encoded/encoded_${{ matrix.chunk }}.mkv
          retention-days: 1

  merge-and-send:
    needs: [split-video, encode-chunks]
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - name: استنساخ المستودع
        uses: actions/checkout@v4

      - name: تجهيز أدوات الدمج
        run: |
          echo "دمج الأجزاء ورفع الملف" > stage.txt
          sudo apt update
          sudo apt install -y ffmpeg p7zip-full
          pip install python-telegram-bot telethon

      - name: تنزيل جميع الأجزاء المضغوطة
        uses: actions/download-artifact@v4
        with:
          pattern: encoded-*
          path: all_encoded
          merge-multiple: true

      - name: تشخيص ودمج الأجزاء
        run: |
          set -euo pipefail
          echo "دمج الأجزاء" > stage.txt
          if [ ! -d all_encoded ] || [ -z "$(ls -A all_encoded 2>/dev/null)" ]; then
            echo "❌ مجلد all_encoded فارغ"; exit 1
          fi
          cd all_encoded
          rm -f concat_list.txt
          for f in $(ls encoded_*.mkv 2>/dev/null | sort); do
            echo "file '$f'" >> concat_list.txt
          done
          [ -s concat_list.txt ] || { echo "❌ لا توجد ملفات"; exit 1; }
          ffmpeg -f concat -safe 0 -i concat_list.txt \
            -c:v copy -c:a libopus -b:a 32k -ac 2 -ar 48000 \
            -y ../output.mkv
          cd ..
          [ -f output.mkv ] && echo "✅ الدمج ناجح" || { echo "❌ فشل الدمج"; exit 1; }

      - name: استخراج معلومات الفيديو
        id: videoinfo
        run: |
          DURATION_SEC=$(ffprobe -v error -show_entries format=duration -of csv=p=0 output.mkv | cut -d. -f1)
          H=$((DURATION_SEC/3600)); M=$(((DURATION_SEC%3600)/60)); S=$((DURATION_SEC%60))
          DURATION_FMT=$(printf "%02d:%02d:%02d" $H $M $S)
          ACTUAL_RES=$(ffprobe -v error -select_streams v:0 -show_entries stream=width,height -of csv=s=x:p=0 output.mkv)
          echo "duration_fmt=$DURATION_FMT" >> "$GITHUB_OUTPUT"
          echo "actual_res=$ACTUAL_RES" >> "$GITHUB_OUTPUT"

      - name: إعداد الاسم النهائي
        id: naming
        env:
          FILENAME: ${{ github.event.inputs.filename }}
          ORIGINAL_TITLE: ${{ needs.split-video.outputs.video_title }}
        run: |
          # أخذ أول سطر فقط وتنظيف الاسم من رموز غير صالحة لأسماء الملفات
          # (تحمي أيضًا من كسر تنسيق GITHUB_OUTPUT عند وجود أسطر متعددة أو رمز |)
          sanitize() {
            printf '%s' "$1" | head -n1 | tr -d '\r' | sed 's/[\/\\:*?"<>|]/ /g' | sed 's/  */ /g' | sed 's/^ *//;s/ *$//'
          }
          SAFE_FILENAME=$(sanitize "$FILENAME")
          SAFE_ORIGINAL=$(sanitize "$ORIGINAL_TITLE")
          if [ -n "$SAFE_FILENAME" ]; then
            FINAL_NAME="$SAFE_FILENAME"
          elif [ -n "$SAFE_ORIGINAL" ]; then
            FINAL_NAME="$SAFE_ORIGINAL"
          else
            FINAL_NAME="video"
          fi
          echo "final_name=$FINAL_NAME" >> "$GITHUB_OUTPUT"

      - name: تشفير (خاص فقط)
        if: github.event.inputs.private == 'true'
        env:
          USER_PASSWORD: ${{ github.event.inputs.password }}
          FINAL_NAME: ${{ steps.naming.outputs.final_name }}
        run: |
          [ -z "$USER_PASSWORD" ] && { echo "❌ كلمة المرور مطلوبة"; exit 1; }
          mv output.mkv "${FINAL_NAME}.mkv"
          7z a -p"$USER_PASSWORD" -mhe=on "${FINAL_NAME}.7z" "${FINAL_NAME}.mkv"

      - name: رفع الملف إلى قناة تيليجرام وإرسال إشعار النجاح
        env:
          TG_API_ID: ${{ secrets.TG_API_ID }}
          TG_API_HASH: ${{ secrets.TG_API_HASH }}
          TG_SESSION: ${{ secrets.TG_SESSION }}
          TG_CHANNEL_ID: ${{ secrets.TG_CHANNEL_ID }}
          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
          CHAT_ID: ${{ github.event.inputs.chat_id }}
          FINAL_NAME: ${{ steps.naming.outputs.final_name }}
          PRIVATE: ${{ github.event.inputs.private }}
          DURATION_FMT: ${{ steps.videoinfo.outputs.duration_fmt }}
          ACTUAL_RES: ${{ steps.videoinfo.outputs.actual_res }}
        run: |
          if [ "$PRIVATE" == "true" ]; then
            UPLOAD_FILE="${FINAL_NAME}.7z"
          else
            UPLOAD_FILE="${FINAL_NAME}.mkv"
            if [ -f "output.mkv" ]; then
              mv output.mkv "$UPLOAD_FILE"
            fi
          fi

          [ -f "$UPLOAD_FILE" ] || { echo "❌ الملف غير موجود: $UPLOAD_FILE"; exit 1; }

          SIZE_MB=$(awk "BEGIN {printf \"%.1f\", $(stat -c%s "$UPLOAD_FILE")/1048576}")
          export UPLOAD_FILE SIZE_MB

          python3 <<'PYEOF'
          import os, asyncio
          from telethon import TelegramClient
          from telethon.sessions import StringSession

          api_id = int(os.environ["TG_API_ID"])
          api_hash = os.environ["TG_API_HASH"]
          session = os.environ["TG_SESSION"]
          channel_id = int(os.environ["TG_CHANNEL_ID"])
          file = os.environ["UPLOAD_FILE"]
          final_name = os.environ["FINAL_NAME"]
          duration = os.environ["DURATION_FMT"]
          resolution = os.environ["ACTUAL_RES"]

          caption = f"🎬 {final_name}\n📐 {resolution}\n⏱️ {duration}"

          async def main():
              client = TelegramClient(StringSession(session), api_id, api_hash)
              await client.start()
              await client.send_file(channel_id, file, caption=caption, force_document=True)
              print("✅ تم رفع الملف إلى القناة بنجاح")

          with TelegramClient(StringSession(session), api_id, api_hash) as client:
              client.loop.run_until_complete(main())
          PYEOF

          python3 <<'PYEOF2'
          import os, asyncio
          from telegram import Bot

          async def main():
              bot = Bot(token=os.environ["BOT_TOKEN"])
              final_name = os.environ["FINAL_NAME"]
              size_mb = os.environ["SIZE_MB"]
              duration = os.environ["DURATION_FMT"]
              resolution = os.environ["ACTUAL_RES"]

              info = f"✅ تم الضغط!\n\n🎬 {final_name}\n📦 {size_mb} MB\n📐 {resolution}\n⏱️ {duration}"
              await bot.send_message(chat_id=os.environ["CHAT_ID"], text=info)
              print("✅ تم إرسال إشعار النجاح")

          asyncio.run(main())
          PYEOF2

      - name: إشعار الفشل
        if: failure()
        env:
          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
          CHAT_ID: ${{ github.event.inputs.chat_id }}
          GITHUB_REPOSITORY: ${{ github.repository }}
          GITHUB_RUN_ID: ${{ github.run_id }}
        run: |
          python3 <<'PYEOF'
          import os, asyncio
          from telegram import Bot
          async def main():
              bot = Bot(token=os.environ["BOT_TOKEN"])
              stage = open("stage.txt").read().strip() if os.path.exists("stage.txt") else "غير معروفة"
              run_url = f"https://github.com/{os.environ['GITHUB_REPOSITORY']}/actions/runs/{os.environ['GITHUB_RUN_ID']}"
              text = f"❌ فشلت العملية.\n🔎 المرحلة: {stage}\n📋 التفاصيل:\n{run_url}"
              await bot.send_message(chat_id=os.environ["CHAT_ID"], text=text)
          asyncio.run(main())
          PYEOF
